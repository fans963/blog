---
import { Navbar as ReactNavbar } from './react/Navbar';

interface Props {
  active?: 'home' | 'blog' | 'about' | 'welcome' | string;
}

const { active = 'home' } = Astro.props;
---

<ReactNavbar client:load active={active} />
