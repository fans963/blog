---
import { NavbarComponent } from './angular/navbar.component';

interface Props {
  active?: string;
}

const { active = 'home' } = Astro.props;
---

<NavbarComponent active={active} client:load />
